import React from "react";

const footer = (props) => {
  return (
    <div
      style={{
        textAlign: "center",
        fontSize: "11px",
        color: "#fff",
        backgroundColor: "#4269ea",
        padding: "5px",
      }}
    >
      @Copyright
    </div>
  );
};

export default footer;
